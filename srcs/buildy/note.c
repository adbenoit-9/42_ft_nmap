

/*
**
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define RDX
**#define ITER_FILE_NEWLINE(root, file, i) do { \
**	((t_nmap_config*)root)->ips = 
**	((char*)((char*)&file)[i]
**	while ()
**	
**}
**while () { \
**	if (nd == NMAP_UDP) { \
**		hook = 1; \
**}	else { \
**		hook = 0; \
**}
**}
**#define ITER_FILE_NEWLINE(root, file) do { \
**
**((t_nmap_setting*)root)-> file\
**\
**}
**#define SELECT_HOOK_PARSY(sockaddr, nd, hook) do
**if (((struct sockaddr_storage *)sockaddr).ss_family == AF_INET) { \
**	if (nd == NMAP_UDP) { \
**		hook = 1; \
**}	else { \
**		hook = 0; \
**}else { \
**	if { \
**		hook = 4; \
**}	else { \
**		hook = 3; \
**	} \
**} \
**} while (0)
*/

